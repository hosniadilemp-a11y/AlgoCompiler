services:
  - type: web
    name: algocompiler
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 120 src.web.app:app
    envVars:
      - key: PYTHONPATH
        value: src
      - key: PYTHONUNBUFFERED
        value: "1"
    autoDeploy: true
